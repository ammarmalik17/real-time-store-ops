version: '3.8'

services:
  store-ops:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
      - ./models:/app/models
    environment:
      - PYTHONPATH=/app
    restart: unless-stopped
    privileged: true  # Needed for camera access in some cases